# SE(2) 位姿估计手推

> 接续《SO2与非线性-高斯牛顿进阶练习.md》。
> SE(2) = 平面位姿 (x, y, θ)，3 个自由度 = SO(2) 的旋转 θ + 平移 (x, y)。
> 本篇目标：把朝向 θ 也从"白送"变成未知数，看它在优化里的行为和位置 (x, y) 有何不同。
> 全程纯文字公式，角度用度/弧度混合时已注明。

---

## 一、和 SO(2) 例题的差别：θ 从"固定 0°"变成未知数

SO(2) 例题里，位姿 = (x, y)，朝向白送成 0°。现在：

```
位姿 = ( x , y , θ )     ← 3 个未知数
```

朝向 θ 也要被估计了。

---

## 二、残差的精确拆解（三个角度）

对任意地标 i，残差牵扯**三个**角度，必须先分清坐标系和"真实/预测"：

| 角度 | 符号 | 精确含义 |
|---|---|---|
| 世界系方向角 | `atan2(ly−y, lx−x)` | 从**当前预测位置** (x,y) 指向地标 i 的连线，相对**世界 +x 轴**的角度 |
| 预测朝向 | θ | 预测位姿里，机体 +x 轴相对世界 +x 轴转了多少 |
| 实测方向角 | α_i | 传感器在**真实位姿**处、以**自身（机体）为参考系**读出的地标方向角（固定读数） |

换算逻辑：传感器在**机体坐标系**里测方向，机器人转 θ 后，它看到的一切整体偏移 −θ。于是：

```
机体系预测方向角 = 世界系方向角 − 预测朝向
              = atan2(ly−y, lx−x) − θ
              = "如果机器人真在预测位姿 (x,y,θ)，传感器会读出什么方向角"

残差 r_i = 机体系预测方向角 − 机体系实测方向角
        = [ atan2(ly−y, lx−x) − θ ] − α_i
```

残差为 0 的唯一条件：预测位姿 = 真实位姿。

---

## 三、雅可比：3 列，θ 列是常数 −1

对地标 i，三个未知数各求一偏导（令 X = lx−x，Y = ly−y，d² = X²+Y²）：

```
∂r_i/∂x  =  Y / d²        （atan2 对位置的导数，非线性）
∂r_i/∂y  = −X / d²        （同上）
∂r_i/∂θ  = −1             （常数！）
```

雅可比行：`J_i = [ Y/d² , −X/d² , −1 ]`。

**关键观察**：θ 在残差里只是被减了一下（−θ），所以 ∂r/∂θ = −1 是常数 → **θ 是线性的**；而 x、y 钻进 atan2 里 → **非线性**。

> 口诀（别记"平移线性、旋转非线性"）：决定线性的不是"平移还是旋转"，而是**变量在表达式里站的位置**——站在加减/乘常数里就是线性，钻进 sin/atan2/相乘里就是非线性。在本方位角模型里恰好反过来：θ 站在减法里（线性），x/y 站在 atan2 里（非线性）。

---

## 四、可观测性：要 3 个地标

1 个地标只给 1 个方位角，3 个未知数 → 至少 **3 个地标**才能把 (x, y, θ) 定死。

---

## 五、第一轮迭代（从 (0,0,0°) 出发）

**题目**：真值位姿 `(1, 1, 30°)`，地标 A(4,1)、B(1,4)、C(4,4)，实测机体系方位角 α = (−30°, 60°, 15°)。

### 5.1 残差

```
r_A = atan2(1,4) − 0 − (−30°) = 14.04° + 30° = 44.04° ≈ 0.769 rad
r_B = atan2(4,1) − 0 − 60°   = 75.96° − 60° = 15.96° ≈ 0.279 rad
r_C = atan2(4,4) − 0 − 15°   = 45° − 15°    = 30°    ≈ 0.524 rad
```

### 5.2 雅可比（每行 `[Y/d², −X/d², −1]`）

```
A：X=4, Y=1, d²=17 → [  1/17  −4/17  −1 ]
B：X=1, Y=4, d²=17 → [  4/17  −1/17  −1 ]
C：X=4, Y=4, d²=32 → [   1/8   −1/8  −1 ]
```

### 5.3 拼 JᵀJ、Jᵀr

```
        [  81/1088    −801/18496   −57/136 ]
JᵀJ =   [ −801/18496    81/1088     57/136 ]
        [  −57/136      57/136       3      ]
      ≈ [  0.074   −0.043   −0.419 ]
        [ −0.043    0.074    0.419 ]
        [ −0.419    0.419    3.000 ]

        [  0.176 ]           [ −0.176 ]
Jᵀr =   [ −0.263 ]   −Jᵀr = [  0.263 ]
        [ −1.571 ]           [  1.571 ]   （第三项 = −(r_A+r_B+r_C) = −π/2）
```

### 5.4 解 3×3 方程（利用矩阵规律消元）

```
① 第1行 + 第2行：(p+q)(δx+δy) = 0.0865   （p+q = 9/289）
                → δx + δy = 2.776
② 第2行 − 第1行：(p−q)(δy−δx) + 2s·δθ = 0.4389
③ 第3行        ：s(δy−δx) + 3·δθ = 1.5708 = π/2

联立②③ → δθ = π/6 = 30°，δy − δx = 0
代回①   → δx = δy = 1.388
```

```
δ = ( 1.388 , 1.388 , 30° )
更新：(x, y, θ) = (1.388, 1.388, 30°)
```

**结果解读**：
- θ 从 0° 一步跳到 30°，**正好落在真值上**——因为 θ 线性，一步就是精确解；
- (x, y) 从 (0,0) 过冲到 (1.388, 1.388)——因为 x/y 非线性。

---

## 六、第二轮迭代（从 (1.388, 1.388, 30°) 出发）

### 6.1 残差

```
r_A = atan2(−0.388, 2.612) − 30° + 30° = −0.148 rad
r_B = atan2(2.612, −0.388) − 30° − 60° = +0.148 rad
r_C = atan2(2.612, 2.612) − 30° − 15° = 0
```

（注意 r_A = −r_B、r_C = 0 的对称性。）

### 6.2 雅可比（新点处重算，数值全变）

```
A：X=2.612, Y=−0.388, d²=6.97  → [ −0.056  −0.375  −1 ]
B：X=−0.388, Y=2.612, d²=6.97  → [  0.375   0.056  −1 ]
C：X=2.612, Y=2.612, d²=13.64  → [  0.191  −0.191  −1 ]
```

### 6.3 拼 JᵀJ、Jᵀr

```
        [  0.180   0.005   −0.510 ]
JᵀJ =   [  0.005   0.180    0.510 ]
        [ −0.510   0.510    3.000 ]

        [  0.064 ]           [ −0.064 ]
Jᵀr =   [  0.064 ]   −Jᵀr = [ −0.064 ]
        [  0.000 ]           [  0.000 ]
```

**关键**：Jᵀr 第三项 = −(r_A + r_B + r_C) = 0，说明 θ 方向上已无残差推力。

### 6.4 解方程

因 δx = δy（记作 d），第三行 → `3δθ = 0` → **δθ = 0**；第一行 → `(0.180+0.005)d = −0.064` → d = −0.343。

```
δ = ( −0.343 , −0.343 , 0 )
更新：(x, y, θ) = (1.045, 1.045, 30°)
```

θ **纹丝不动**（δθ = 0），x/y 折返收向 1.0。

---

## 七、收敛表

| 迭代 | x | y | θ | r_A | r_B | r_C | 代价 F |
|---|---|---|---|---|---|---|---|
| 0 | 0 | 0 | 0° | +0.769 | +0.279 | +0.524 | 0.943 |
| 1 | 1.388 | 1.388 | 30° | −0.148 | +0.148 | 0 | 0.044 |
| 2 | 1.045 | 1.045 | **30°** | −0.015 | +0.015 | 0 | 0.0005 |

（第三轮再把 x/y 压到 ≈1.001，θ 继续不动。）

---

## 八、结论

把朝向 θ 加进来，它只是**线性地**多了一个未知数：

1. **θ 一轮到位、之后零修正**（线性，∂r/∂θ = −1 常数）；
2. **x/y 过冲、折返、逐步收敛**（非线性，钻进 atan2）。

SE(2) 例题一句话：**真正要反复迭代的，始终是钻进 atan2 里的位置 (x,y)；朝向 θ 只是"线性地"搭了个便车。** 这再次印证了"记站哪，不记平移 vs 旋转"的口诀。
