# 激光-惯导 SLAM 与 LIO-SAM 学习笔记

> 整理自与 Claude 的讨论，面向大三学生，从零开始讲清 SLAM 与 LIO-SAM。
> 全程使用通俗语言 + 纯文字公式。

---

## 目录

1. [SLAM 是什么](#一slam-是什么)
2. [传感器：LiDAR 与 IMU](#二传感器lidar-与-imu)
3. [松耦合 vs 紧耦合](#三松耦合-vs-紧耦合)
4. [LIO-SAM 的两大核心思想](#四lio-sam-的两大核心思想)
5. [LIO-SAM 四大模块](#五lio-sam-四大模块)
6. [LIO-SAM 的优势与局限](#六lio-sam-的优势与局限)
7. [学习路径建议](#七学习路径建议)
8. [因子图核心概念澄清（重点）](#八因子图核心概念澄清重点)
9. [IMU 预积分深入（含热启动）](#九imu-预积分深入含热启动)
10. [高斯-牛顿手动迭代示例](#十高斯-牛顿手动迭代示例)
11. [为什么一个节点会有多个 IMU 因子](#十一为什么一个节点会有多个-imu-因子)
12. [激光相对位移怎么算：扫描匹配](#十二激光相对位移怎么算扫描匹配)
13. [地图是轨迹的副产品](#十三地图是轨迹的副产品)
14. [全流程：从第一帧到持续运行](#十四全流程从第一帧到持续运行)

---

## 一、SLAM 是什么

**SLAM = Simultaneous Localization And Mapping，同时定位与建图。**

想象你被蒙上眼睛扔进一个陌生房间，手里只有一根拐杖（能敲到墙知道距离）。你要同时做两件事：

1. 搞清楚自己在房间的哪个位置（定位）；
2. 用拐杖把房间的布局摸出来（建图）。

这是一个「先有鸡还是先有蛋」的困境：

- 要建图，得先知道「我站在哪」，才能把探测到的墙放在正确位置；
- 要知道「我站在哪」，又得先有张地图来对照。

SLAM 就是同时解决这两者：机器人在未知环境中一边移动，一边估计自己的轨迹，一边构建环境地图。

**为什么不用 GPS？** 因为 GPS 在室内、地下、隧道、城市峡谷、水下等场景信号差甚至失效，而 SLAM 恰恰为这些环境设计。这也是自动驾驶、扫地机器人、仓储物流、AR/VR 离不开 SLAM 的原因。

---

## 二、传感器：LiDAR 与 IMU

### 2.1 激光雷达 LiDAR（眼睛）

- **原理**：发射激光，测光打到物体再返回的时间（飞行时间法），乘以光速除以 2 得距离。转一圈得到周围点云。
- **优点**：测距准（厘米级）、不受光照影响、直接给三维几何信息。
- **缺点**：频率相对低（机械式约 10 Hz）；在退化场景失效（笔直长走廊、空旷无特征场地）。

### 2.2 惯性测量单元 IMU（内耳）

- **组成**：
  - 加速度计：测线性加速度（含重力分量）；
  - 陀螺仪：测角速度。
- **优点**：频率高（100–1000 Hz），能捕捉快速运动，不依赖外部参考。
- **缺点**：会漂移。测量带噪声和零偏（bias），两次积分后误差随时间二次增长，几分钟就飘。

> 关键结论：激光准但慢、怕退化；IMU 快但会漂。所以要融合互补。

---

## 三、松耦合 vs 紧耦合

| | 松耦合 | 紧耦合 |
|---|---|---|
| 做法 | 各自先算出结果再拼一起（如 EKF 滤波融合） | 把原始测量放进同一个优化问题一起解 |
| 优点 | 简单、模块化、好调试 | 信息利用充分，精度鲁棒性高 |
| 缺点 | 只用中间结果，丢失信息 | 复杂、计算量大 |
| 代表 | 早期 LOAM 思路 | **LIO-SAM**、FAST-LIO |

LIO-SAM 是**紧耦合**，这是它精度高、鲁棒性强的根本原因。

---

## 四、LIO-SAM 的两大核心思想

### 4.1 因子图（Factor Graph）

把待估计的状态画成图：

```
● ──── ● ──── ● ──── ●
  因子   因子   因子
（节点 = 某时刻状态，边 = 连接状态的约束/测量）
```

- **节点（变量）**：每个关键帧的状态（位置、姿态、速度、IMU 零偏）；
- **边（因子）**：某个测量给出的「这些状态应满足的关系」。

整个问题变成：**找一组节点值，让所有边的「矛盾」最小** —— 一个非线性最小二乘优化问题。LIO-SAM 用 Google 的 GTSAM 库搭图并优化。

不同来源的测量（IMU、激光、GPS、回环）统一建模成因子塞进同一张图，这就是「紧耦合」的实现方式。

### 4.2 IMU 预积分（Preintegration）

**问题**：IMU 要两次积分才能得位置，但积分起点（上一时刻状态）在优化中会不断被调整，每次调整都要重算积分。

**预积分的做法**：不直接积出绝对位置，而是把两帧之间 IMU 测量**预先积成一个相对量**（姿态转了多少、速度变了多少、位置移了多少），这个相对量不依赖起始状态的绝对值。于是：

- IMU 数据被压缩成「两帧之间的相对运动约束」因子；
- 优化时即使起点变了，这个相对量基本不用重算（只小幅线性修正）。

> 一句话：预积分把一堆高频 IMU 数据打包成两帧间一个紧凑约束。

---

## 五、LIO-SAM 四大模块

```
                ┌─────────────────────────────┐
 激光雷达 ─────►│ 特征提取模块                  │──► 角点/面点
                └─────────────────────────────┘
                                                   │
                ┌─────────────────────────────┐    ▼
    IMU ───────►│ IMU预积分模块                 │  初始位姿/IMU里程计
                └─────────────────────────────┘    │
                                                   ▼
                ┌─────────────────────────────┐
                │ 地图优化模块（因子图+增量优化）│◄── 核心
                └─────────────────────────────┘
                              ▲
                              │ 回环约束
                ┌─────────────┴───────────────┐
                │ 回环检测模块                  │
                └─────────────────────────────┘
```

### 模块 1：特征提取

- 计算每个点的曲率（roughness，看周围点是否排成线或面）；
- **角点**：曲率大，是棱、边、墙角；
- **面点**：曲率小，是平整地面、墙面。
- 用少量角点+面点代表整帧点云，匹配又快又稳。
- 继承 LeGO-LOAM 的地面分割，把地面点单独处理。

### 模块 2：IMU 预积分

- 在激光两帧之间，以几百 Hz 高频更新姿态位置，得到高频里程计；
- 用预积分打包帧间相对运动约束，作为因子；
- 估计并修正 IMU 零偏。

两大作用：
1. 提供**初始位姿猜测**给激光匹配；
2. 激光退化场景下靠 IMU 短时间顶住。

### 模块 3：地图优化（核心）

搭因子图并增量优化。图中因子包括：
1. IMU 预积分因子（约束相邻关键帧相对运动）；
2. 激光里程计因子（当前帧与局部地图匹配得到的位姿约束）；
3. GPS 因子（可选，绝对位置约束）；
4. 回环因子（历史重访约束）。

关键机制：
- **关键帧**：位姿变化超阈值（如移动 1m 或转 10°）才新建，控制图规模；
- **滑窗**：维护局部地图窗口，只和附近局部地图匹配，保证实时；
- **边缘化**：老状态退出滑窗时把信息折叠成先验，不直接扔；
- **增量优化（ISAM2）**：不全图重算，只更新受影响部分。

> 「Smoothing And Mapping」的由来：用平滑/优化（而非滤波）同时做里程计和建图。

### 模块 4：回环检测

1. 用 **Scan Context**（把点云编码成指纹/描述子）快速检索历史相似帧；
2. 找到候选后用 **ICP**（迭代最近点）精细配准，算出当前与历史帧的相对位姿；
3. 作为强约束因子加进图，把整条轨迹和地图「拉」得闭合，消除累积漂移。

---

## 六、LIO-SAM 的优势与局限

**优势：**
1. 紧耦合 + 因子图，信息利用充分；
2. IMU 补盲：快速运动、退化场景靠 IMU 提供先验；
3. 激光纠漂：地图匹配定期把 IMU 拉回正确位置；
4. 回环消漂：保证全局一致；
5. 关键帧 + 滑窗 + 边缘化：精度与实时平衡。

**局限：**
- 依赖激光特征，纯退化场景久了仍漂；
- 参数需标定（雷达、IMU 型号）；
- 室内无 GPS 因子时长时间大范围漂移仍存在。

---

## 七、学习路径建议

### 需要补的数学（按重要程度）

1. 线性代数（旋转矩阵、齐次坐标、特征值）；
2. 李群李代数 SO(3)/SE(3)（姿态参数化、扰动求导，是硬骨头）；
3. 概率与贝叶斯（高斯分布、协方差、最大后验）；
4. 非线性优化（最小二乘、高斯-牛顿、LM、雅可比）。

### 推荐资料

- 高翔《视觉 SLAM 十四讲》（前几章的李代数、优化、因子图通用，强烈推荐先读）；
- Cyrill Stachniss 的 SLAM 公开课；
- Thrun《Probabilistic Robotics》；
- 论文顺序：LOAM(2014) → LeGO-LOAM(2018) → LIO-SAM(2020) → FAST-LIO(2021)；
- GTSAM 文档。

### 动手实践

- 装 Ubuntu + ROS1/ROS2；
- 下载 KITTI 数据集；
- 跑通 LIO-SAM，改参数看效果；
- 读源码，对照论文四个模块的入口函数。

---

## 八、因子图核心概念澄清（重点）

> 这一部分是我们讨论中最绕、也最重要的一块，单独整理。

### 8.1 真实轨迹唯一，为什么还要「找一组节点」？

物理世界的真实轨迹**是唯一的**，这是客观事实。但关键在于：**这个唯一真实轨迹我们永远看不到**，我们手里只有带噪声的测量。

- 节点不是「已知事实」，是**想求却不知道的未知数**；
- 「找一组节点」不是说现实有多解，而是「从一堆不完美测量里反推出唯一真相」。

为什么「矛盾最小」而不是「矛盾为零」？
1. 测量有噪声，任何一组节点都无法让所有测量精确满足；
2. 测量冗余且互相不一致，只能求总体上最吻合。

时间顺序的作用：决定图的**拓扑**（谁连谁），但不决定节点的**数值**。

### 8.2 因子是什么？由节点算出来吗？

因子**不是**靠节点算出来的，而是**来自外部测量数据**。

```
残差 = 预测测量值(节点) − 实际测量值(固定数据)
     =      h(节点)       −        z
```

- 因子同时依赖两样东西：节点（通过预测这一项）、测量值 z（来自传感器，固定不动）；
- 优化时只调整节点，z 提前算好、焊死不动；
- 矛盾来自「根据节点预测出的」与「传感器实际测到的」之差。

### 8.3 测量不是真实值（最易错的点）

**SLAM 里根本没有「真实值」可用**，真实轨迹正是要去找的未知东西。

- 测量值是带噪声、有误差的，不全信；
- 预测值是从假设的节点值反推的；
- 真相既不等于测量值也不等于预测值，在两者之间，优化器去找。

> 直觉检验：IMU 说走 0.98 米，激光说走 1.03 米，真实走 1.00 米。我们不知道哪个更接近真相，只知道它们打架，让优化器按可靠性折中。正因为测量不是真实值，才有矛盾，才需要「最小化矛盾」。

### 8.4 平滑 vs 滤波

| | 滤波（EKF 等） | 平滑/因子图（LIO-SAM） |
|---|---|---|
| 思路 | 一步步走：预测当前→测量纠正 | 所有节点一次性摆出，全局一起优化 |
| 「预测」 | 真的「从前面推后面」 | 只是「由节点算出该有的测量」，无方向性 |
| 特点 | 顺序、只保留当前状态 | 全局、保留历史、回环可修正整条轨迹 |

### 8.5 节点由什么组成？（易误解点）

节点**不是**「由传感器参数拼成」的。节点 = 机器人在某时刻的**状态**，是一组纯未知数字：

```
一个节点的状态（约 15 个未知数）：
  · 位置    x, y, z
  · 姿态    roll/pitch/yaw
  · 速度    vx, vy, vz
  · IMU零偏  陀螺仪零偏、加速度计零偏
```

里面没有任何一条传感器读数。传感器读数在**因子**里。节点最终数值确实**间接**算出来的（通过优化解方程），但「不是由传感器参数拼成」，而是「被传感器测量约束出来」。

### 8.6 紧耦合「一起看」的是什么？

不是「把传感器参数拼成状态」，而是：**同一个节点同时被多个传感器的因子从不同方向拉扯，全部放进同一个优化问题一起解。**

比喻：节点是漂浮的木桩，IMU/激光/GPS 因子是连在不同方向的三根橡皮筋，各自往测到的位置拉；紧耦合就是三根筋同时拉这一个木桩，停在平衡位置。

### 8.7 没有「预测」，怎么和测量比较？（核心答疑）

预测**是存在的**，只是方向相反：预测的是**测量值**，不是下一个节点。

每个因子做的事：拿当前节点取值，反算出「这个传感器该测到什么」，再和「实际测到的」比。

- **当前优化的结果** = 节点的当前取值；
- **测量值** = 每个因子各自的固定传感器读数 z；
- **预测** = h(节点)，节点一改，h(节点) 就变，残差就变；z 永远不动。

优化就是不停改节点、让「预测测量」逼近「实际测量」。

#### 1D 数字例子

机器人沿直线动，只关心位置 x。三个节点 x0、x1、x2。初始猜测 x0=0, x1=1.1, x2=2.0。

测量：
- IMU（相对位移）：z01 =「x0→x1 移 +1.0」，z12 =「x1→x2 移 +0.9」；
- GPS（绝对位置）：z_gps =「x2 在 2.2」。

逐个因子算残差：

**因子 1：IMU 连 (x0,x1)**
- 预测测量 h = x1 − x0 = 1.1 − 0 = 1.1
- 实际测量 z01 = 1.0
- 残差 = +0.1

**因子 2：IMU 连 (x1,x2)**
- 预测 h = x2 − x1 = 2.0 − 1.1 = 0.9
- 实际 z12 = 0.9
- 残差 = 0

**因子 3：GPS 连 (x2)**
- 预测 h = x2 = 2.0
- 实际 z_gps = 2.2
- 残差 = −0.2

总代价 = 0.1² + 0² + (−0.2)² = 0.05。

优化器调整 x0/x1/x2，让总代价变小，直到收敛。

### 8.8 回环不是特例

回环因子和普通因子用**同一套机制**，唯一区别是连接**哪两个节点**：

- IMU 因子连相邻节点；
- 回环因子连任意两个节点（如 x0 和 x100）；
- GPS 因子只连一个节点。

无论连谁，公式都一样：残差 = h(节点) − z。回环的 z 来自历史点云与当前点云配准出的相对位姿。回环的意义在于连**远距离**节点，能把误差摊到中间一整串节点上。

### 8.9 怎么知道优化有没有效？

看**总代价**。优化每一步让总代价变小（或持平）；收敛到最小值、梯度归零（无论单独动哪个节点总代价都压不下去）时，就找到了最优解。有效性靠「总代价是否收敛」判断，不靠和真值比（没真值）。

---

### 8.10 节点不是「测量出来」的，是「估计」出来的

节点（状态）**不是传感器测出来的**，而是我们想求、却还不知道、要靠估计的未知量。

节点里是位置、姿态、速度、零偏这些数，没有哪个传感器能直接「测出」姿态角或速度：

- IMU 直接测到的是**加速度**和**角速度**，不是速度、不是姿态——要得到速度得积分加速度，要得到姿态得积分角速度，而积分正是漂移的根源；
- 激光直接测到的是**距离**（点云）；
- GPS 直接测到的是**位置**（还带噪声）。

所以状态是「由一堆测量反推、估计出来的」。传感器只能提供「约束」，不能直接告诉我们状态是多少。如果节点是测出来的，那还优化什么呢？直接读出来就完事了。

### 8.11 误差到底从哪来：不同测量之间打架

一个关键疑问：既然「预测」是从节点反推、节点又是从数据算出来的，那不是「同一个来源」吗？为什么会有矛盾？

答案：预测的依据不是「这一条测量」，而是「节点」，而节点是**所有测量（不同传感器、不同时刻）的折中**。这些测量互相矛盾（都有噪声），折中的节点无法精确复现任何一条测量，于是对某一条因子，「预测 ≠ 测量」。

用 1D 例子看矛盾本身：

- IMU 说 x1 − x0 = 1.0、x2 − x1 = 0.9；
- GPS 说 x2 = 2.2。

由 IMU 链推 x2 = x0 + 1.0 + 0.9 = 1.9；由 GPS 推 x2 = 2.2。**两条不同来源对同一个 x2 给出不同答案——这就是矛盾本身。**

本质是解一个「超定方程组」（方程比未知数多、互相矛盾，没有精确解）：

- x = 1.0（传感器 A）
- x = 1.2（传感器 B）
- x = 0.9（传感器 C）

一个未知数、三个互相矛盾的方程，没有精确解，只能求「让总误差最小的 x」。求出来之后每条方程都对不齐，残差就是这么来的。

> 只有一条测量时，反推回去误差恒为零；SLAM 有误差，恰恰因为测量冗余且互相矛盾。

### 8.12 「重新优化」和「预测比较」是同一件事

新测量来了，直接「重新做优化」就行——这正是平滑的做法。但要澄清：**「预测 → 比较」不是优化之前的一个额外步骤，它就是优化本身。**

「重新优化」= 重新求让总误差最小的节点；而算总误差，就必须对每个因子做「预测 h(节点) − 测量 z」。所以：

- 从外面看：新数据来了 → 重新优化 → 得到新结果；
- 从里面看：重新优化 = 反复「对每个因子：预测 − 测量 → 调节点」。

两者是同一个动作。不存在「要么直接重新优化、要么先预测再比较」的二选一。

### 8.13 热启动与两种「预测」

容易混淆的两个「预测」：

1. **测量预测 h(节点)**：用节点算「传感器本该读到什么」，是算误差的一部分，依据的是「这个因子连的那几个节点」；
2. **状态预测（初始值 / 热启动）**：新节点刚加进来时值还未知，需要给个起点。这个起点常用「上一个节点 + IMU 相对运动」来估。

第二种不是必需的哲学步骤，只是省事技巧：给个靠谱起点，优化收敛快、不容易陷进局部极值。就算给随便一个初值，优化器最终也能找到答案，只是更慢、更容易卡住。

---

## 九、IMU 预积分深入（含热启动）

### 9.1 预积分是什么（1D 视角）

IMU 直接测到的是**加速度**，不是位移。位移 = 加速度积分两次（先积出速度，再积出位置）。

两帧之间（第 k 到第 k+1 帧），IMU 以几百 Hz 吐出一串加速度，积分两次得到两帧间的相对位移：

```
Δx = x_{k+1} − x_k
```

这个「把两帧之间的 IMU 数据积成一个相对位移」的过程就是预积分。

### 9.2 为什么叫「预」积分

- **普通积分**：从 x_k 的绝对值出发积出 x_{k+1}，**依赖 x_k**——x_k 在优化中一变，后面整段积分都要重算；
- **预积分**：只积「相对量 Δx」，**不依赖 x_k 的绝对值**——算一次，优化时无论节点怎么调都能复用。

1D 里这看起来理所当然（位移本来就与起点无关），但 3D 里要把加速度积分成速度/位置需要用到姿态，而姿态也在被估计、在变，所以「先积成与绝对状态无关的相对量」才不平凡、才配叫「预积分」。

### 9.3 它同时干两件事

预积分算出的 Δx 一鱼两吃：

1. 当 **IMU 因子的测量值 z**（「这两帧相对位移 = 0.9」的约束）；
2. 当 **新节点的热启动初值**（x_{k+1} = x_k + Δx）。

### 9.4 热启动时 IMU 残差为什么是 0

因为新节点初值 x_{k+1} = x_k + Δx，而 Δx 正好是 IMU 因子的测量值 z，所以：

- IMU 因子预测 = x_{k+1} − x_k = Δx，测量 z = Δx → **残差 = 0**。

矛盾没消失，只是全部压在了其它传感器（如 GPS）的残差上。优化开始后，节点被拉到折中位置，IMU 残差才不再是 0。

> 一般原则：用哪个测量做热启动，那个因子的残差就从 0 起步；其它传感器的矛盾全部塞进它们自己的残差里；优化就是把这些残差重新「摊平」。

---

## 十、高斯-牛顿手动迭代示例

### 10.1 摆好问题

- 参考点 x0 = 0（固定不动）；
- 待求 x1、x2；
- 测量：IMU01（x1 − x0 = 1.0）、IMU12（x2 − x1 = 0.9）、GPS（x2 = 2.2）。

热启动（顺着 IMU 链）：x1 = 1.0，x2 = 1.9。

### 10.2 初始残差

- r1 = x1 − 1.0 = 0
- r2 = x2 − x1 − 0.9 = 0
- r3 = x2 − 2.2 = −0.3

总代价 = 0² + 0² + (−0.3)² = 0.09。

### 10.3 雅可比 J（每个残差对每个节点的拉动方向）

```
        [ ∂r1/∂x1   ∂r1/∂x2 ]   [  1   0 ]
J   =   [ ∂r2/∂x1   ∂r2/∂x2 ] = [ −1   1 ]
        [ ∂r3/∂x1   ∂r3/∂x2 ]   [  0   1 ]
```

### 10.4 高斯-牛顿一步

JᵀJ = [2 −1; −1 2]，Jᵀr = [0; −0.3]。

解 (JᵀJ)δ = −Jᵀr 得 δ1 = 0.1、δ2 = 0.2。

更新：x1 ← 1.0 + 0.1 = 1.1，x2 ← 1.9 + 0.2 = 2.1。

新残差：r1 = 0.1、r2 = 0.1、r3 = −0.1，总代价 0.03。

### 10.5 误差被摊平了

| | r1 | r2 | r3 | 平方和 |
|---|---|---|---|---|
| 优化前 | 0 | 0 | −0.3 | 0.09 |
| 优化后 | +0.1 | +0.1 | −0.1 | 0.03 |

总绝对误差没变（0.3），但从「全压在 GPS」变成「三条各 0.1」；一坨 0.3 的平方和（0.09）大于三个 0.1 的平方和（0.03）——这正是最小二乘喜欢「摊平误差」的数学原因。

### 10.6 为什么这一步直接到位

因为这个 1D 例子是**线性**的（J 里全是常数），一步就精确。真实 3D SLAM 里有旋转、残差对姿态非线性，J 会随估计值变化，所以要反复「重新算 J、走一步」——「一步步挪」的来源就是非线性。

> 高斯-牛顿 = 算残差 r → 算雅可比 J → 解 (JᵀJ)δ = −Jᵀr 得到 δ → 更新节点。

---

## 十一、为什么一个节点会有多个 IMU 因子

**先纠正一个心理模型：因子是一条「边」，连接两个节点，而不是「挂」在某一个节点上的东西。**

拿一条链举例，节点 X0、X1、X2、X3：

```
X0 ──F01── X1 ──F12── X2 ──F23── X3
```

- F01 是「X0→X1 的相对运动」，连 X0 和 X1；
- F12 连 X1 和 X2；F23 连 X2 和 X3。

看中间节点 **X1**，它被**两条** IMU 因子碰到：一条「进来」（F01），一条「出去」（F12）。之前容易只数到「进来」那条（上一帧连到这一帧），漏了「出去」那条。

> **除了第一个和最后一个节点，每个中间节点都被两条 IMU 因子夹住——一条进、一条出。**

另一个容易产生「多个」错觉的点：两帧之间 IMU 有几百个原始读数，但预积分的作用正是把这几百个读数**压成一条边**。所以不会出现「几百个 IMU 因子」，永远是一帧间一条。

> 补充：在 X1 和 X2 之间，其实**不只有 IMU 因子，还有一条激光里程计因子**。这两条因子连着**同样两个节点**，从不同方向拉它们——这正是第 8.6 节「三根橡皮筋」比喻在真实系统里的样子：同一对节点被 IMU 和激光同时约束，互相打架，优化器折中。

---

## 十二、激光相对位移怎么算：扫描匹配

激光的帧间相对位移**不是积分出来的**，用的是另一种算法——**点云配准 / 扫描匹配（scan matching）**。角色上像 IMU 预积分（都产出「帧间相对运动」当因子），机制完全不同。

### 机制：不是积分，是「找对齐」

1. 手里有当前帧的**角点 + 面点**（模块 1 的产出），还有一张**局部地图**（最近几个关键帧的特征拼成）；
2. 找一个变换 T（旋转 + 平移），把「当前帧特征」搬到「局部地图」上，让它们尽量对齐；
3. 「对齐」的判据是距离：每个**角点**算它到地图里对应**线**的距离（点到线）；每个**面点**算它到对应**面**的距离（点到面）；
4. 让所有这些距离的平方和最小，解出来的 T 就是这一帧的**激光相对位姿**。

这第 3、4 步本身就是一个最小二乘问题——**和因子图用的是同一套高斯-牛顿/列文伯格-马夸尔特**，只不过是个更小的局部问题。所以激光里程计因子 = 一个「扫描匹配」解出的相对位姿 T，当成这条因子的测量值 z。

### 和 IMU 预积分的对照

| | IMU 预积分 | 激光里程计（扫描匹配） |
|---|---|---|
| 机制 | 积分：把加速度/角速度积成相对运动 | 配准：找让点云对齐的变换 T |
| 输入 | 几百 Hz 的 IMU 读数 | 当前帧角点/面点 + 局部地图 |
| 输出 | 相对位移 Δx | 相对位姿 T（旋转+平移） |
| 要不要初值 | 不用（积分不需要猜） | 要（迭代优化必须有起点） |
| 角色 | 因子测量值 z | 因子测量值 z |

注意「要不要初值」——**这正是模块 2 的「角色 1：给初始位姿」存在的理由**。扫描匹配是迭代优化，没有起点会收敛慢、甚至陷进局部极值；IMU 预积分算出的高频里程计正好当这个起点：

```
IMU ──预积分──► 初值 T₀（猜个大概）──┐
                                     ├─► 扫描匹配迭代修正 ──► 精确 T（激光里程计因子）
激光 ──特征提取──► 角点/面点 ────────┘
```

---

## 十三、地图是轨迹的副产品

### 被优化的不是「激光数据」，是「位姿」

激光点云本身**没有被优化**，它是一份固定的测量值，从头到尾不变。被优化的是**位姿 T**——即「拍这帧点云时，传感器在世界的哪里、朝哪」。

```
传感器坐标里的点（固定不变）× 优化后的位姿 T = 世界坐标里的点（地图）
```

地图 = 用优化后的位姿，把每一帧固定不变的点云摆到世界坐标系里，一帧帧拼起来。点没变，变的是「把它们摆到哪」。所以地图不是「反推」出来的，而是「摆放/拼接」出来的——正向操作，不是反演。

### 这个模型的两个结论

1. **地图不用单独优化**：轨迹解出来那一刻，地图「自动」就有了——把每帧点云按位姿摆进去，零额外计算。这也是 LIO-SAM 高效的原因之一：只优化几千个位姿，不优化几百万个地图点。

2. **地图是「有弹性」的**：回环纠正了轨迹后，地图不用重新匹配，只要用纠正后的位姿重新摆一遍旧点云，整张地图就「啪」地对上。地图的一致性完全由轨迹决定。

### 回扣「鸡生蛋」

SLAM 的相互依赖之所以难，是因为看起来「定位需要地图、建图需要定位」。LIO-SAM 的解法不是「先解一个再解另一个」，而是**把两个问题合并成「一个」问题——只估计轨迹（位姿序列）。地图不作为独立未知量，而是轨迹的副产品。** 于是「鸡生蛋」被消解：没有两个互相依赖的未知量，只有一个未知量（轨迹），地图从它身上「长」出来。

（细节：优化过程中，扫描匹配确实会拿一张「局部小地图」来对齐当前帧，那张局部地图又是用之前的位姿拼的——所以过程中定位和建图仍是交织迭代的；但最终交付的全局地图，就是「最终轨迹 + 点云」的副产品。）

---

## 十四、全流程：从第一帧到持续运行

### 三个「算」：先拆开

在因子图内部有「预测值 → 和实际值比 → 优化」这套动作，但在因子图之前还有两个「算」发生，它们产出因子图要用的「实际测量值 z」：

| 层级 | 做什么 | 产出 |
|---|---|---|
| A. IMU 预积分 | 把两次激光之间的 IMU 读数积分成相对运动 | IMU 因子的 z，同时也是新位姿的初值 |
| B. 扫描匹配 | 拿初值，把当前帧点云和局部地图对齐，解出相对位姿 | 激光里程计因子的 z |
| C. 因子图优化 | 拿所有因子的 z，优化节点 | 轨迹 |

激光不会像 IMU 那样「积分」，它要靠对齐点云才得到相对位姿，而且对齐本身也要用 IMU 给的初值。所以 A→B→C 环环相扣。

### 第一阶段：启动（第一帧）

- 机器人此刻位姿人为定为原点（位置 0、姿态 0），这就是第一个节点 X0；
- 若开机静止，先用这段时间估计 IMU 零偏；
- 第一帧点云做完特征提取后，其角点/面点直接进局部地图，作为后续匹配的参照。

### 第二阶段：每一帧激光进来后（约 10 Hz）

1. **IMU 预积分（一直在跑，几百 Hz）**：把上一帧到这一帧之间积攒的 IMU 读数积分成一个相对运动量（转多少、速度变多少、位置移多少）。层级 A。
2. **点云去畸变**：激光转一圈要时间，过程中车在动，点云会「糊」。用高频 IMU 把这一帧内每个点的位姿补正。IMU 的第三个用途。
3. **特征提取**：把去畸变后的点云抽成角点 + 面点（模块 1）。
4. **初始位姿猜测（热启动）**：这一帧位姿初值 = 上一关键帧位姿 + IMU 预积分的相对运动。这只是初值，不是最终答案。
5. **扫描匹配（层级 B）**：拿第 4 步初值当起点，把这一帧角点/面点和局部地图对齐，迭代优化（高斯-牛顿）最小化「角点到线、面点到面」距离，解出精确相对位姿 T。**这个 T 就是激光里程计因子的测量值 z。**

### 第三阶段：是不是关键帧

- 位姿变化够大（移动 > 1m 或转 > 10°）就定为新关键帧，进入下一阶段；
- 否则不新建节点，只用它更新局部地图，避免图爆炸。

### 第四阶段：新关键帧 → 加节点 + 加因子

6. 加一个新节点 X_k（状态 = 位姿、速度、IMU 零偏）；
7. 加两条因子连到上一关键帧 X_{k-1}：IMU 预积分因子（z = 第 1 步的相对运动）、激光里程计因子（z = 第 5 步的 T）；
8. 有 GPS 加 GPS 因子；有回环加回环因子。

### 第五阶段：因子图优化（层级 C，ISAM2）

9. 增量优化，不全图重算。对每条因子做 `预测 h(节点) − 测量 z = 残差`，让所有残差按噪声加权后的总和最小。输出优化后的轨迹。
10. 更新局部地图：用优化后位姿把最近几个关键帧特征重新摆好，供下次扫描匹配用。
11. 更新全局地图：用优化后位姿把每个关键帧的完整点云摆进世界坐标，拼成最终地图（副产品）。

### 第六阶段：回环检测（并行）

- Scan Context 把当前帧编码成指纹，在历史帧里检索相似者；
- 找到候选后 ICP 精细配准，算出当前帧与历史帧的相对位姿；
- 作为回环因子（连远距离节点）加进图，触发全局重优化，把轨迹拉闭合。

> 完整一句话：每来一帧，IMU 积分出相对运动（既是初值、又是 IMU 因子测量值）→ 用初值做扫描匹配解出激光里程计因子测量值 → 若够格则新建节点、把因子接进图 → 因子图做「预测 vs 实测」最小二乘优化 → 得到优化后轨迹 → 地图作为副产品拼出来。

---

## 一句话总结

**SLAM 要解决的是：真实轨迹唯一但看不到，手里只有一堆带噪声、互相不完全一致的测量。把待求状态当变量（节点），把每个测量变成一个约束（因子），因子的形式是「由节点算出的预测测量」与「传感器实测值」的残差。找一组节点值，让所有残差按各自噪声加权后的总和最小，得到的就是对真实轨迹的最好估计。**
