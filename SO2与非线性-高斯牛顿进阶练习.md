# SO(2) 与「非线性」到底在哪 —— 高斯-牛顿进阶练习

> 接续《激光-惯导 SLAM 与 LIO-SAM 学习笔记》第十节。
> 目标是回答 10.6 那句"真实 3D SLAM 有旋转、J 会随估计值变化"——非线性到底藏在哪里、为什么一步不准、以及为什么还需要列文伯格-马夸尔特(LM)。
> 全程纯文字公式，弧度与角度混用时都已注明。

---

## 一、先纠正一个误解：纯旋转在 2D 里其实"几乎线性"

SO(2) = 平面旋转 = 一个角度 θ，只是要处理"350° 和 −10° 是同一个旋转"这件事。

两个算子：

- **加法 ⊕**：θ₁ ⊕ θ₂ = θ₁ + θ₂（注意 SO(2) 是"交换"的：先转 30° 再转 20°，和先 20° 再 30° 一样）
- **减法 ⊖**（求两个朝向差多少）：θ₁ ⊖ θ₂ = wrap(θ₁ − θ₂)，把结果压回 (−180°, 180°]

关键点：wrap 的导数等于 1（除了 ±180° 那个接缝处）。所以一个"纯旋转"残差

```
r = (θ_预测) ⊖ (θ_测量)
```

对 θ 求导**还是 1 —— 雅可比是常数**。也就是说，只转不动的 2D 问题，和第十节那个 1D 例子一样"一步到位"，几乎没有非线性。

> 结论：非线性**不是**"旋转"这个量本身带来的，而是**角度/旋转"钻进测量模型"时带来的**——比如方位角测量里有 atan2，比如 3D 里旋转矩阵出现在点的坐标变换里。下面用方位角测量把这件事看清楚。

---

## 二、方位角测量：非线性真正现形

### 问题设置

- 机器人站在未知位置 (x, y)，朝向固定为 0°（朝 +x 轴），这里只估计位置，简化起见。
- 两个已知地标：A 在 (4, 1)，B 在 (1, 4)。
- 机器人测到地标的方位角（相对自身朝向）：对 A 是 0°，对 B 是 90°。（无噪声；真值其实在 (1, 1)）

预测方位角 = 从机器人指向地标的向量角度：

```
h_A(x,y) = atan2( 1 − y , 4 − x )
h_B(x,y) = atan2( 4 − y , 1 − x )
```

残差 = 预测 − 实测：

```
r_A = atan2(1 − y, 4 − x) − 0°
r_B = atan2(4 − y, 1 − x) − 90°
```

对比第十节的 1D 残差（r = x1 − 1.0，是一条**直线**），这里的 atan2 是一条**曲线**——这就是非线性。

### 雅可比（关键：它依赖 (x, y)）

对 atan2(Y, X)，其中 Y = ly − y，X = lx − x，d² = X² + Y²（到地标的距离平方）：

```
∂atan2/∂x =  Y / d²
∂atan2/∂y = −X / d²
```

注意 d²、X、Y 全由 (x, y) 决定 → **雅可比随估计值变化**。这正是 10.6 说的"J 会随估计值变，要反复重算"。

### 初始猜测 (0, 0)，第一次迭代

残差（弧度）：

- r_A = atan2(1, 4) − 0 ≈ **0.245**
- r_B = atan2(4, 1) − π/2 ≈ **−0.245**

雅可比（在 (0,0)：对 A，X=4, Y=1, d²=17；对 B，X=1, Y=4, d²=17）：

```
J = (1/17) [ 1   −4 ]
           [ 4   −1 ]
```

JᵀJ = (1/289)[17 −8; −8 17]，Jᵀr = [−0.043; −0.043]。

解 (JᵀJ)δ = −Jᵀr 得 **δ = [1.388, 1.388]**。

更新：(x₁, y₁) = (0, 0) + (1.388, 1.388) = **(1.388, 1.388)**。

**过冲了！** 真值在 (1, 1)，一步却跨到了 (1.39, 1.39)。原因：高斯-牛顿用"切线"去近似曲线，切线往外飞得比曲线本身更远。

### 第二次迭代

新残差：r_A ≈ −0.147，r_B ≈ +0.147（比 0.245 小，在变好，但没归零）。

新雅可比（在 (1.388, 1.388) 处）：

```
J = [ −0.056   −0.374 ]
    [  0.374    0.056 ]
```

解出 **δ = [−0.343, −0.343]**，更新：(x₂, y₂) = (1.388 − 0.343, 1.388 − 0.343) = **(1.045, 1.045)**。

已经逼近真值 (1, 1)，再来一两步就收敛。

### 收敛轨迹：之字形逼近

```
(0,0) ──过冲──► (1.388, 1.388) ──折返──► (1.045, 1.045) ──► (1, 1)
```

---

## 三、从这里带走三件事

1. **平移是线性的，角度/旋转钻进测量模型才非线性。** 1D 例子一步到位，因为残差是直线；方位角残差是 atan2 曲线，所以要一步步挪。

2. **J 随估计值变**，所以每一步都要用"当前估计"重新算 J——这就是"迭代"二字的数学含义，也是 ISAM2 反复做局部线性化的原因。

3. **高斯-牛顿会过冲**，上例一步跨过头就是证明。列文伯格-马夸尔特(LM) 干的事，就是在 (JᵀJ) 上加一个阻尼 λI，把步长"勒住"：既防过冲，也防 JᵀJ 接近奇异时炸掉。第七节里并列提到的"高斯-牛顿、LM"，到这里你终于有了 LM 的直觉。

---

## 四、升到 SO(3) 之前还差一小步

把朝向 θ 也变成未知量（不再固定为 0），残差就变成：

```
r_i = atan2( ly_i − y , lx_i − x ) − θ − α_i(实测)
```

这就是完整的 **SE(2)** 平面 SLAM 问题（位姿 = x, y, θ）。做完这一步，再升到 **SO(3)**（3D 旋转），⊕ 和 ⊖ 就不再是"角度加减"，而是"矩阵相乘 + log 映射"了——那才是李代数的入口，也是你第七节里标记的"硬骨头"。
